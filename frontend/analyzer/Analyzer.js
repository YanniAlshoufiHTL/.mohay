class Analyzer{
    constructor(){
        this = new Analyzer();
    }
    analyze(code){
        let lines = code.split("\n");
        let variables = [];
        let constants = [];

        for(let line of lines){
            switch(line){
                case line[0] === ".":
                    //this is a variable

                    break;
        }


    }
}
    reqeust(line, char){
        return "yeet";
    }
}